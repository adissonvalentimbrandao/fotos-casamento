services:
  - type: web
    name: fotos-casamento
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    dockerContext: .
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
    # Disco persistente (planos pagos na Render):
    # disk:
    #   name: fotos-data
    #   mountPath: /var/data
    #   sizeGB: 1
    # + env: DATA_DIR=/var/data
