# Local (opcional) — copie para .env
PORT=3000
NODE_ENV=development

# Render define automaticamente (não configure no painel):
# PORT=10000
# RENDER_EXTERNAL_URL=https://fotos-casamento.onrender.com
# RENDER=true

# Produção com Docker na Render (já no render.yaml):
# DATA_DIR=/var/data
